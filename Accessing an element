# get vs [] for retrieving elements
my_dict = {'name': 'shivani', 'age': 17}

# Output: shivani
print(my_dict['name'])

# Output: 17
print(my_dict.get('age'))

# Trying to access keys which doesn't exist throws error
# Output None
print(my_dict.get('address'))

# KeyError
print(my_dict['address'])
